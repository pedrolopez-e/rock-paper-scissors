/*Function that simulates the computer play*/
function computerPlay() {
    let strings = ["Rock", "Paper", "Scissors"];
    let play = strings[Math.floor(Math.random()*strings.length)];
    return play;
}

function round() {
    
}
